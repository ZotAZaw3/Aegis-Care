# Logs
logs
*.log
.claude/prompt-log.jsonl
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Secrets — private/service-role keys go in .dev.vars or *.local (ignored below).
# The tracked .env holds ONLY public VITE_ anon keys. NEVER put a service-role
# key (bypasses RLS) in .env — it would sync to git + Lovable.
.env.local
.env.*.local

# Claude Code local config, hooks, and prompt log
.claude/settings.local.json
.claude/hooks/
.claude/prompt-log.json
.claude/prompt-log.json.corrupt-*

# Synthea dataset (3.34GB — do not commit)
synthea-dental-dataset/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
